#include "../Inc/cube.h"
#include <math.h>

void handle_rotation() {
}

void handle_movement() {
}

void move_camera(t_scene *scene, t_map *map) {
    (void)scene;
    (void)map;
   handle_rotation();
   handle_movement();
}
